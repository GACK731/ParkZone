<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Transparent inside -->
    <solid android:color="@android:color/transparent" />

    <!-- Border color and width -->
    <stroke
        android:width="2dp"
        android:color="#4671fd" />

    <!-- Rounded corners -->
    <corners android:radius="25dp" />
</shape>
